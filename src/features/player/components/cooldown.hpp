namespace features::player::components {
    struct cooldown {
        float value;
        cooldown(float _value)
            : value(_value)
        {
        }
    };
}  // namespace features::player::components